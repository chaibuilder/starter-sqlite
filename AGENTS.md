# Agents

This project uses the Payload CMS skill at `.agents/skills/payload/`.
Start with `.agents/skills/payload/SKILL.md` for a quick reference, then see `.agents/skills/payload/reference/` for detailed docs.

This project is a collaboration of PaylodCMS and ChaiBuilder.
ChaiBuilder skills are at `pro/.agents/chaibuilder/SKILL.md`

Naming:
- Always use CHAIBUILDER or ChaiBuilder. never use with spaces or underscore to separate two words.